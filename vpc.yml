dev:
  securityGroupIds:
    - ${opt:security_group_id, ''}
  subnetIds:
    - ${opt:subnet_id_1, ''}
    - ${opt:subnet_id_2, ''}
    - ${opt:subnet_id_3, ''}

stg:
  securityGroupIds:
    - ${opt:security_group_id, ''}
  subnetIds:
    - ${opt:subnet_id_1, ''}
    - ${opt:subnet_id_2, ''}
    - ${opt:subnet_id_3, ''}

prod:
  securityGroupIds:
    - ${opt:security_group_id, ''}
  subnetIds:
    - ${opt:subnet_id_1, ''}
    - ${opt:subnet_id_2, ''}
    - ${opt:subnet_id_3, ''}